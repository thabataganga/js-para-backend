// var

/* var altura = 5;
var comprimento = 7;

area = altura * comprimento;
console.log(area); */

// let

/* let forma = 'retangulo';
let altura = 5;
let comprimento = 7;

if (forma === 'retangulo') {
    area = altura * comprimento
} else {
    area = (altura * comprimento) / 2;
}

console.log(area); */

// const

// Esse tipo de varialvel não pode ser alterada dentro do programa, é uma constante -> serve para proteger variaveis que não podem ser alteradas

const forma = 'triangulo';
const altura = 5;
const comprimento = 7;
let area;

if (forma === 'quadrado') {
    area = altura * comprimento
} else {
    area = (altura * comprimento) / 2;
}

P4ss4pr4m1m *
    console.log(area);